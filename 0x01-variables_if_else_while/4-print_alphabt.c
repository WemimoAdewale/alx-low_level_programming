#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main- Entry point
*
* Return:Always 0 sucess
*/
int main(void)
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
{
if (ch != 'q' && ch != 'e')
putchar(ch);
}
i
